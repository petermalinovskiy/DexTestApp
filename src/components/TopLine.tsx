import React from "react";
import { View } from "react-native";
import { globalStyles } from "../../styles/Styles";

export const TopLine = () => {
  return (
    <View style={globalStyles.topLine}/>
  )
}